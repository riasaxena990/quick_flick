<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white"/>
    <corners android:radius="5dp"/> <!-- No corner radius for a rectangle -->
    <stroke
        android:width="3dp"
        android:color="@color/black" /> <!-- You can change the color as needed -->
    <padding
        android:left="10dp"
        android:right="10dp"
        android:top="10dp"
        android:bottom="10dp"/>
</shape>
